---
layout: base.njk
---
{# this isn't part of the base template because it fights with the postlist css #}
{% css %}{% include "css/lines.css" %}{% endcss %}

<article>
  {% if hero.image and hero.alt %}
    <img class="hero" src="{{ hero.image }}" alt="{{ hero.alt }}">
  {% endif %}

  {{ content | safe }}
</article>
